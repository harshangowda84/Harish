# Copy to .env and fill values
# For local dev without Docker/Postgres we use SQLite. Copy this file to `.env`.
# To use Postgres instead, replace DATABASE_URL with the Postgres connection string.
DATABASE_URL="file:./dev.db"
JWT_SECRET=replace_with_a_secret
PORT=4000
